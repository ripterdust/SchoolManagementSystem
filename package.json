{
  "name": "school_management",
  "version": "1.0.0",
  "description": "This is an app build from scratch with ruby on rails for the backend and angular for the frontend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently -n \"CLIENT,SERVER\" \"cd client && ng serve\" \"cd server && rails s\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
